# Global.gd
# Script "global" du jeu
# La visibilité des variables et fonctions sont limités à sa scène, et à ses enfants.
# Lorsque l'on veut communiquer entre des scènes, il faut utiliser des variables globales, ou des fonctions globales.
# Pour cela, il faut créer un script "global" qui sera accessible depuis toutes les scènes.
# Après des tests, nous ne sommes pas arrivés à faire fonctionner des paramètres globaux.
# C'est pourquoi ce script est vide.
extends Node

# Fonction appelée lorsque le noeud à fini de s'initialiser
func _ready():
	pass 

